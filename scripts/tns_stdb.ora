#tnsnames file from AWS Quick Start template

QS_PRIMARY_NAME =
  (description =
    (address = (protocol = tcp)(host = QS_PRIMARY_IP)(port = QS_DATABASE_PORT))
    (connect_data =
      (server = dedicated)
      (service_name = QS_PRIMARY_NAME)
    )
  )

QS_STANDBY_NAME =
  (description =
    (address = (protocol = tcp)(host = QS_STANDBY_IP)(port = QS_DATABASE_PORT))
    (connect_data =
      (server = dedicated)
      (service_name = QS_STANDBY_NAME)
    )
  )
